# CCFTS 项目进展笔记 — 2026-05-31

## 当前状态

**Phase 0（架构）+ Phase 1（法规层）+ Phase 2（扩展）全部完成。** 7 个旧技能全部迁移至 Base+SLOT，5 个组织层级全覆盖，3 个工作流文件 + 合并报表技能 + workflow-base v0.2 已就位。

## 已完成清单

### Phase 0 — 架构搭建
- [x] 目录结构：5职能领域 × 5组织层级
- [x] `skills/README.md` — 导航地图
- [x] `skills/_template-skill.md` — SLOT就绪的技能模板
- [x] `scripts/validate-skills.py` v0.2 — domain/level/slug/references 全面校验
- [x] `mcp/ccfts_mcp/server.py` v0.2 — slug别名 + list_intelligence + resolve_references
- [x] MCP `pyproject.toml`

### Phase 1 — 12个法规技能（全部通过验证）
- [x] SASAC ×4：快报时间表、决算、一利五率、国有资本经营预算
- [x] MOF ×3：CAS 14收入、CAS 33合并、小企业准则
- [x] STA ×4：增值税法2026、金税四期、全电发票、CIT预缴
- [x] MOHURD ×2：资质体系、安全生产费

### Phase 1 MVP 遗留（8个技能，待Phase 2重构）
- [x] foundation/ccfts-workflow-base.md
- [x] china-construction/ ×7：实体类型、科目表、快报映射、取整、利润表、B/S、期末调整

## 验证结果
```
21 files, 0 issues
所有交叉引用有效
0个真实企业名称残留
```

## Phase 2 Base+SLOT 试点（2026-05-31 完成）

**取整规则**是第一个落地 Base+SLOT 模式的技能，验证了架构可扩展性：

| 文件 | 位置 | 角色 |
|---|---|---|
| `ccfts-fr-all-rounding-rules` | `financial-reporting/_base/` | Base 文件，定义 10 个 SLOT 占位符 + 通用工作流 |
| `ccfts-fr-spv-rounding-rules` | `financial-reporting/spv/` | SPV 覆盖（投资管理类/Type A） |
| `ccfts-fr-project-unit-rounding-rules` | `financial-reporting/project-unit/` | 项目部覆盖（施工总承包类/Type B） |

### 试点验证结论
- ✅ Base+SLOT 模式工作正常，10 个 SLOT 全部正确填充
- ✅ 验证脚本通过（24 文件，0 问题）
- ✅ MCP 服务器 slug 别名和 intent 目录已更新
- ✅ 跨层引用链正确：`fills_slots_for` → `references` → `depends_on`
- ✅ 验证脚本已升级：支持多段连字符层级名（`project-unit`）+ `fills_slots_for: null` 处理

### 发现的问题（已修复）
1. **Validator 无法解析多段层级名**：`project-unit` 含连字符，`split("-")` 会把 `project-unit` 拆成 `project` + `unit`。修复：逐段尝试匹配 `ALLOWED_LEVELS`
2. **`fills_slots_for: null` 被解析为字符串 `"null"`**：YAML null 被简单解析器识别为 truthy 字符串。修复：Validator 过滤 `"null"/"none"/""` 字符串

## 下一步：Phase 2 继续

目标：财务报表全域扩展（~15个新技能）

1. 3个 `_base/` 工作流文件（快报、决算、合并）
2. 跨层级文件：主体类型扩展、取整规则扩展、期末调整扩展
3. 5个组织层级快报技能：soe-group、subsidiary、branch、project-unit、spv
4. 合并报表技能
5. 升级 workflow-base 到 v0.2

## 关键设计决策（新会话需要知道）

1. **职能领域主维度，组织层级子维度** — skills/ 按 domain（fr/acct/tax/anlys/mgmt/intel）组织
2. **Base + SLOT 模式** — `_base/` 文件定义通用工作流+命名SLOT占位符，层级覆盖文件填充SLOT
3. **命名规范** — `ccfts-{domain}-{level}-{topic}.md`
4. **脱敏原则** — 所有技能文件不出现真实企业名称/项目名称/地名
5. **向后兼容** — 旧 slug 通过 MCP SLUG_ALIASES 映射到新 slug
6. **三阶段迁移** — 旧技能保持不变并行开发 → 新技能齐备后重构 → MCP slug别名

## 关联项目
- `financial-reporting-automation/` — 现有 Python 转换工具，99.2% 匹配率，是回归测试基准
- `20_references/obsidian-index-vault/` — 王家湾项目索引
- `30_prompts/shared/` — 共享提示词和写作规范

## 研究已归档
- 国资委/SASAC：快报制度、决算管理、一利五率考核、国有资本经营预算
- 财政部/MOF：CAS 14收入（五步法）、CAS 33合并（控制三要素）、小企业准则
- 国税局/STA：增值税法2026（六大变化）、金税四期（五流合一）、全电发票、CIT预缴
- 住建部/MOHURD：资质体系（三序列）、安全生产费（计提1.5-3.5%）
- 组织层级：CREC/CRCC/CSCEC 的集团→工程局→工程公司→项目部/总包部/代局指→项目公司SPV 全链条
